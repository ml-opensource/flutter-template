# Register bricks which can be consumed via the Mason CLI.
# Run "mason get" to install all registered bricks.
# To learn more, visit https://docs.brickhub.dev.
bricks:
  # Bricks can be imported via version constraint from a registry.
  # Uncomment the following line to import the "hello" brick from BrickHub.
  # hello: 0.1.0+1
  # Bricks can also be imported via remote git url.
  # Uncomment the following lines to import the "usecase" brick from git.
  # usecase:
  #   git:
  #     url: https://github.com/monstar-lab-oss/flutter-bricks.git
  #     path: bricks/usecase
  
  # service brick
  service:
    git:
      url: https://github.com/monstar-lab-oss/flutter-bricks.git
      path: bricks/service

  # usecase brick
  usecase:
    git:
      url: https://github.com/monstar-lab-oss/flutter-bricks.git
      path: bricks/usecase

  # feature brick    
  feature:
    git:
      url: https://github.com/monstar-lab-oss/flutter-bricks.git
      path: bricks/feature